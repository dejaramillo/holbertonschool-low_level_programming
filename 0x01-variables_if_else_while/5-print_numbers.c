#include <stdio.h>
/**
 * main - print the number cwith printf
 *
 * return: loop with number
 */
int main(void)
{
int numbers;
while (numbers <= 9)
{
printf("%d", numbers);
numbers++;
}
printf("\n");
return (0);
}
